# Variables de la app para el build de producción (npm run build). No contiene secretos.
# Servidor Express publicado en Render (pagos automáticos y correo de recuperación)
VITE_API_URL=https://mavi-api.onrender.com
